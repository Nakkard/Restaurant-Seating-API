# Local development only. Choose a strong password (12+ characters, upper/lower case, digits, symbols).
# Avoid semicolons and dollar signs here: this value is also used in a connection string and Compose.
SQL_SERVER_PASSWORD=ReplaceWithYourLocalPassword123!
SQL_SERVER_PORT=14333
API_PORT=5080
